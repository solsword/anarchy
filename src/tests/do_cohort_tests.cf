// vim: syntax=c
/**
 * @file: do_cohort_tests.cf
 *
 * @description: Code fragment for calling tests in tests/cohort_tests.cf.
 *
 * @author: Peter Mawhorter (pmawhorter@gmail.com)
 */

unit_test("find_cohort", &test_find_cohort);

unit_test("find_cohort_and_inner", &test_find_cohort_and_inner);

unit_test("cohort_interleave", &test_cohort_interleave);

unit_test("cohort_fold", &test_cohort_fold);

unit_test("cohort_spin", &test_cohort_spin);

unit_test("cohort_flop", &test_cohort_flop);

unit_test("cohort_shuffle", &test_cohort_shuffle);

unit_test("cohort_shuffle_visual", &test_cohort_shuffle_visual);

unit_test("find_mixed_cohort", &test_find_mixed_cohort);

unit_test("find_mixed_cohort_and_inner", &test_find_mixed_cohort_and_inner);

unit_test("mixed_cohort_visual", &test_mixed_cohort_visual);

unit_test("find_biased_cohort_and_inner", &test_find_biased_cohort_and_inner);

unit_test("biased_cohort_visual", &test_biased_cohort_visual);

unit_test("test_exp_split_visual", &test_exp_split_visual);

unit_test("exp_cohort_sections_visual", &test_exp_cohort_sections_visual);

unit_test("exp_cohort_visual", &test_exp_cohort_visual);

unit_test("exp_cohort_inners", &test_exp_cohort_inners);

unit_test("find_exp_cohort_and_inner", &test_find_exp_cohort_and_inner);
